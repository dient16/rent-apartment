// Global stylesheets are side-effect imports
declare module '*.css';

// Image imports are typed by Next itself (next/image-types/global) and resolve to
// StaticImageData — read `.src` when passing one to a plain <img>.
